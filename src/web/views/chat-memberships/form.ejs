<%- include('../layout', { body: `
<div class="page">
  ${(function() {
    const escapeHtml = (str) => {
      if (!str) return '';
      return String(str)
        .replace(/&/g, '&amp;')
        .replace(/</g, '&lt;')
        .replace(/>/g, '&gt;')
        .replace(/"/g, '&quot;')
        .replace(/'/g, '&#39;');
    };
    const displayName = escapeHtml(channel.display_name || channel.twitch_username);

    return `
  <div class="page-header">
    <div class="page-header-content">
      <a href="/channels/${channel.id}/chat-memberships" class="back-link">&larr; Back to Chat Memberships</a>
      <h1 class="page-title">Join Chat</h1>
      <p class="page-subtitle">${displayName}</p>
    </div>
  </div>

  <div class="page-content">
    <form action="/channels/${channel.id}/chat-memberships" method="POST">
      <input type="hidden" name="_csrf" value="${csrfToken}">

      <div class="card mb-6">
        <div class="card-header">
          <h3 class="card-title">Channel to Join</h3>
        </div>
        <div class="card-body">
          <div class="form-group">
            <label for="target_channel" class="form-label">Twitch Username</label>
            <div class="input-group">
              <span class="input-group-text">@</span>
              <input type="text" name="target_channel" id="target_channel"
                     class="form-input"
                     placeholder="channelname"
                     pattern="[a-zA-Z0-9_]+"
                     required
                     autocomplete="off">
            </div>
            <p class="form-hint">Enter the Twitch username of the channel you want the bot to join.</p>
          </div>
        </div>
      </div>

      <div class="card mb-6">
        <div class="card-header">
          <h3 class="card-title">Notes</h3>
        </div>
        <div class="card-body">
          <ul class="text-secondary" style="list-style: disc; padding-left: 1.5rem;">
            <li>The bot will attempt to join the specified channel's chat.</li>
            <li>Commands and counters configured for your channel will work in joined chats.</li>
            <li>The bot joins your own channel automatically - you don't need to add it here.</li>
            <li>Make sure the channel name is spelled correctly.</li>
          </ul>
        </div>
      </div>

      <div class="form-actions">
        <button type="submit" class="btn btn-primary">Join Chat</button>
        <a href="/channels/${channel.id}/chat-memberships" class="btn btn-secondary">Cancel</a>
      </div>
    </form>
  </div>
    `;
  })()}
</div>
`, activePage: 'memberships', channel: channel }) %>
